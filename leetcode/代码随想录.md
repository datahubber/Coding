# 数组
704. Binary Search
while left <= right:
  mid = (left+right)//2
if, elif, else之后加":",下一行缩进
nums[mid]

278. first bad version
            if target > nums[mid]:
                left = mid + 1
            else:
                right = mid
        
        return left
